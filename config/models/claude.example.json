{
   "model": "claude-fable-5",
   "description": "Claude Fable — fast, creative, general purpose. Routed via the default gateway (DEFAULT_BASE_URL / DEFAULT_API_KEY).",
   "omitParams": ["temperature"]
}
