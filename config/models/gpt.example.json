{
   "model": "gpt-5.5",
   "description": "OpenAI GPT 5.5 — fast, broad knowledge. Called directly with its own endpoint and key.",
   "baseUrl": "https://api.openai.com/v1",
   "apiKey": "env:OPENAI_API_KEY",
   "omitParams": ["temperature"]
}
